{
  "name": "hq-reader-vercel",
  "version": "2.0.3",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspace hq-reader-frontend",
    "dev": "npx vercel dev",
    "check": "node --check server/catalog.js && node --check server/googleDrive.js && node --check server/archiveCache.js && node --check api/comics.js && node --check api/content.js && node --check api/blob-upload.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "adm-zip": "^0.5.16",
    "node-unrar-js": "^2.0.2"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
