# Desenvolvimento: deixe vazio para usar o proxy do Vite (/api -> localhost:3001).
# Produção (Vercel): informe a URL pública do backend Render, sem barra final.
# Exemplo: VITE_API_BASE_URL=https://hq-reader-api.onrender.com
VITE_API_BASE_URL=
